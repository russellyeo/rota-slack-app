{
  "name": "rota-slack-app",
  "version": "1.0.0",
  "description": "Manage a list of rotas in your workspace",
  "author": "Russell Yeo",
  "license": "MIT",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc -w -p .",
    "start": "npm run build && node dist/app.js",
    "lint": "eslint . --ext .ts,.js --ignore-pattern node_modules",
    "test": "jest"
  },
  "engines": {
    "node": ">=17.9.1",
    "npm": ">=8.11.0"
  },
  "dependencies": {
    "@slack/bolt": "^3.11.3",
    "axios": "^1.4.0",
    "dotenv": "~16.0.3",
    "yargs": "^17.7.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.4",
    "@types/jest": "^29.5.3",
    "@types/node": "^14.14.35",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.9.5"
  }
}
